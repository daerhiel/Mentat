import { LexiconController } from './lexicon-controller';

describe('LexiconController', () => {
  it('should create an instance', () => {
    expect(new LexiconController(null!, null!, null!)).toBeTruthy();
  });
});
